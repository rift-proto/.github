= Rift

[quote]
**Rift Realtime Protocol (Rift/1)** — a modern, real-time bidirectional messaging protocol built on WebSocket, designed to replace raw `JSON.stringify`-over-socket patterns with typed frames, explicit delivery semantics, and production-grade session management.

== Why Rift?

=== Built for AI Realtime Interaction

Rift is purpose-built for the AI streaming era. LLM inference, agent orchestration, and
real-time collaboration require more than raw WebSocket — they need typed message classes,
stream-aware backpressure, and session resilience that survives network hiccups mid-generation.

* **Stream segments** (`StreamSegment`) — native support for ordered, chunked delivery.
  Perfect for LLM token streaming, file transfers, and long-running agent tasks.
  Each segment carries a monotonic `seq` and a `final_segment` flag — no manual framing needed.
* **Command / Reply RPC** — request-response over the same connection with `correlation_id`,
  timeout control, and idempotency keys. Ideal for AI inference calls and tool-use round trips.
* **Backpressure control** — AI responses can be arbitrarily long. Rift's 6 backpressure
  strategies (pause, drop volatile, coalesce state, downgrade, disconnect, snapshot-later)
  prevent fast producers from overwhelming slow consumers.
* **Session resume** — epoch-based reconnect with per-topic offset replay.
  An AI conversation survives a network drop and resumes exactly where it left off,
  no context lost.
* **Datagram class** — fire-and-forget messages for typing indicators, cursor positions,
  and streaming status updates. Dropped first under backpressure — never blocks the main stream.
* **State + Presence** — latest-only state messages for collaborative editing, agent status,
  and live cursors. Presence tracks who's online with automatic TTL expiry.

=== Comparison with Socket.IO and SSE

[cols="1,1,1,1"]
|===
| Feature | SSE | Socket.IO | Rift/1

| Direction
| Server → Client only
| Bidirectional
| Bidirectional

| Transport
| HTTP/1.1 or HTTP/2
| WebSocket + polling fallback
| WebSocket (native)

| Encoding
| Text only (UTF-8)
| JSON only
| **CBOR + JSON** negotiated

| Stream segments
| Ad-hoc `data:` lines
| Manual chunking
| **`StreamSegment`** with seq, final_segment, schema

| RPC / Request-Reply
| Not possible
| Manual correlation
| **`Command` + `Reply`** with correlation_id, timeout, idempotency

| Delivery guarantees
| None (fire-and-forget)
| Fire-and-forget
| **6 modes** (AtMostOnce → DurableOrdered)

| Session resume
| Auto-reconnect, no state
| Basic sid cookie
| **Epoch-based** resume with per-topic offset tracking

| Backpressure
| None
| None
| **6 strategies** + token-bucket rate limiting

| Message types
| Event only
| Event only
| **8 classes** (Event, Command, Reply, State, Datagram, Stream, Snapshot, System)

| Topic management
| URL path
| String namespace
| **Profiles** with retention, ordering, rate limits

| Binary support
| No
| No (JSON string)
| **CBOR native** — 30-50% smaller payloads for structured data

| AI streaming fit
| Workable but fragile
| Adequate
| **First-class** — Stream segments, Command/Reply RPC, backpressure

| Connection lifecycle
| Implicit
| Basic connect/disconnect
| **Hello → Welcome → Resume → Ready → Active → Draining → Closed**
|===

SSE is a fine choice for simple server-push notifications, but it breaks down for
bidirectional AI agent workflows. Socket.IO adds bidirectional messaging but lacks
stream-aware delivery, backpressure, and typed message semantics.

Rift gives you the best of both worlds: WebSocket efficiency with protocol-level
guarantees purpose-built for AI realtime — streaming tokens, agent tool calls,
multi-turn conversations that survive reconnection, and backpressure that keeps
fast models from overwhelming slow browsers.

== Connection Lifecycle

```text
Client                              Server
  │                                     │
  │──── Hello ───────────────────────>│  capabilities + auth
  │<─── Welcome ──────────────────────│  session_id + codec
  │   [optional Resume exchange]        │
  │<─── Ready ───────────────────────│  limits + heartbeat
  │                                     │
  │ ═══ bidirectional data frames ════  │
  │                                     │
  │──── Close ───────────────────────>│  (either side)
```

== Repositories

[cols="1,2"]
|===
| Repository | Description

| https://github.com/rift-proto/rifts[rifts]
| Rust server implementation. Standalone WebSocket + framework adapters (Axum, Actix Web, Warp, Ntex).

| https://github.com/rift-proto/rift-ts[rift-ts]
| TypeScript client SDK for browser and Node.js.
|===

== Protocol Highlights

* **Typed frame envelope** — single `Frame` structure for all wire traffic, with priority (6 levels), 10 flag bits, and routing/correlation metadata
* **CBOR + JSON codec negotiation** — binary encoding for production, JSON for debugging, negotiated once at handshake
* **8 message classes** — Event, Command (RPC), Reply, State (latest-only), Datagram (best-effort), Stream (ordered segments), Snapshot, System
* **Topic profiles** — per-topic retention policy (None / TTL / Count / Size / Durable / Latest), ordering policy (None / Connection / Publisher / Topic / Key / Causal), and rate limits
* **Session resume** — transport-agnostic logical sessions with epoch validation and per-topic offset replay
* **Acknowledgement system** — 9 status levels (Received → Processed), 6 policies (None → Application)
* **Flow control** — backpressure with 6 strategies (Pause / DropVolatile / CoalesceState / Downgrade / Disconnect / SnapshotLater) + token-bucket rate limiter
* **Heartbeat** — ping/pong with jitter to prevent thundering-herd, configurable missed-pong threshold
* **30 error codes** and **16 close codes** — structured, machine-readable, with retryability semantics

== TPL Specifications

The complete Rift/1 wire protocol is specified as **TPLs** (Technical Protocol Listings) in the `tpl/` directory. See link:tpl/README.adoc[tpl/README.adoc] for the reading guide.

[cols="1,2,1"]
|===
| TPL | Title | Status

| link:tpl/TPL-FRAME-1.adoc[TPL-FRAME-1]
| Frame Envelope
| `mandatory`

| link:tpl/TPL-CODEC-1.adoc[TPL-CODEC-1]
| Codec Negotiation
| `mandatory`

| link:tpl/TPL-MSG-1.adoc[TPL-MSG-1]
| Message Types
| `mandatory`

| link:tpl/TPL-TOPIC-1.adoc[TPL-TOPIC-1]
| Topic Profiles
| `mandatory`

| link:tpl/TPL-SUB-1.adoc[TPL-SUB-1]
| Subscribe Modes
| `mandatory`

| link:tpl/TPL-ACK-1.adoc[TPL-ACK-1]
| Acknowledgement System
| `mandatory`

| link:tpl/TPL-SESSION-1.adoc[TPL-SESSION-1]
| Session & Resume
| `mandatory`

| link:tpl/TPL-HANDSHAKE-1.adoc[TPL-HANDSHAKE-1]
| Hello / Welcome / Ready Handshake
| `mandatory`

| link:tpl/TPL-AUTH-1.adoc[TPL-AUTH-1]
| Authentication
| `mandatory`

| link:tpl/TPL-FLOW-1.adoc[TPL-FLOW-1]
| Flow Control
| `mandatory`

| link:tpl/TPL-HEARTBEAT-1.adoc[TPL-HEARTBEAT-1]
| Heartbeat
| `mandatory`

| link:tpl/TPL-ERROR-1.adoc[TPL-ERROR-1]
| Error Codes
| `mandatory`

| link:tpl/TPL-CLOSE-1.adoc[TPL-CLOSE-1]
| Close Codes
| `mandatory`

| link:tpl/TPL-VERSION-1.adoc[TPL-VERSION-1]
| Protocol Versioning
| `mandatory`

| link:tpl/TPL-CONFIG-1.adoc[TPL-CONFIG-1]
| Server Configuration
| `optional`
|===

== License

Dual-licensed under MIT or Apache-2.0.
